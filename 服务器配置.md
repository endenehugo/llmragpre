# LLM RAG 阿里云 Ubuntu 服务器部署指南

> 目标服务器：阿里云 ECS `47.92.74.156`（Ubuntu 24.04）
> 项目路径：`/opt/llmrag`

---

## 一、项目现状速览

| 项 | 现状 |
|---|---|
| 框架 | Flask 3.0.0，通过 `app/__init__.py` 创建 `app` 实例 |
| 生产入口 | `wsgi.py` → `from app import app` |
| Gunicorn 配置 | 已存在 `gunicorn.conf.py`：单 worker、gthread、180s 超时 |
| 配置加载 | `FLASK_ENV` 环境变量 → `config/config_{env}.py` |
| 数据库 | MySQL（SQLAlchemy + PyMySQL） |
| 向量库 | FAISS（CPU 版） |
| LLM | 阿里云 DashScope（qwen-plus / qwen-vl-plus / text-embedding-v3） |
| 前端 JS | `credentials.js` 已用 `window.location.origin + "/"`，无需改 |

### ⚠️ 上线前必须处理的安全问题

1. **`config/config_prod.py` 里硬编码了 `DASHSCOPE_API_KEY` 和 `METASO_API_KEY`**，请立即更换并通过环境变量注入。
2. **`run.py` 的 `debug=True`** 仅限本地开发，生产环境用 Gunicorn 启动，不会触发这行。
3. **`README.md` 说建 `.env` 就行，但 `__init__.py` 的 `load_dotenv()` 只加载到 `os.environ`，实际配置值由 `config_prod.py` 的 `os.environ.get()` 读取**——所以两种方式都有效，但建议统一用系统环境变量（systemd `Environment=`），避免 `.env` 文件泄露。

---

## 二、从 Windows 上传项目到服务器

在本地 Windows PowerShell 执行：

```powershell
scp -r F:\code\llmrag root@47.92.74.156:/opt/
```

> 如果不是 root 用户，替换为你的用户名。上传后 SSH 登录：

```bash
ssh root@47.92.74.156
```

---

## 三、服务器环境安装

### 3.1 系统依赖

```bash
apt update
apt install -y python3 python3-venv python3-pip nginx mysql-server git
```

> 一行装完所有必需组件。下面分别说明 MySQL 和 Git 的安装细节。

#### 安装 Git

Git 通过上面的 `apt install -y git` 已安装。验证：

```bash
git --version
# 输出类似：git version 2.43.0
```

配置用户信息（用于后续 git commit）：

```bash
git config --global user.name "你的名字"
git config --global user.email "你的邮箱"
```

#### 安装 MySQL

Ubuntu 24.04 上 `apt install mysql-server` 直接安装 MySQL 8.x。如果安装过程中没有弹出密码设置界面，MySQL 8 默认使用 `auth_socket` 插件，root 用户只能通过 `sudo mysql` 登录。

```bash
# 查看 MySQL 是否运行
systemctl status mysql

# 没运行则启动
systemctl enable mysql
systemctl start mysql
```

首次登录 MySQL（Ubuntu 24.04 默认使用 socket 认证）：

```bash
sudo mysql
```

如果你希望用密码登录 root（可选），进入 MySQL 后执行：

```sql
ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY '你的root密码';
FLUSH PRIVILEGES;
```

> 改完后用 `mysql -u root -p` 登录。如果后续遇到 `Access denied for user 'root'@'localhost'`，先用 `sudo mysql` 进去检查 `authentication_string` 和 `plugin` 是否正确。

### 3.2 创建 Python 虚拟环境

```bash
cd /opt/llmrag
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
```

### 3.3 安装项目依赖

```bash
pip install -r requirements-prod.txt
```

> `requirements-prod.txt` 已包含 gunicorn。如果某些包安装失败，先检查 Python 版本（需 ≥ 3.10，推荐 3.11）。

### 3.4 初始化 MySQL

```bash
systemctl enable mysql
systemctl start mysql
```

创建数据库和用户（按你的 `config_prod.py` 配置）：

```sql
mysql -u root
CREATE DATABASE llmrag CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'llmrag'@'localhost' IDENTIFIED BY '你的密码';
GRANT ALL PRIVILEGES ON llmrag.* TO 'llmrag'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

然后初始化表结构：

```bash
cd /opt/llmrag
source .venv/bin/activate
python setup_mysql.py
```

### 3.5 验证能启动

```bash
export FLASK_ENV=prod
/opt/llmrag/.venv/bin/gunicorn -w 1 -b 0.0.0.0:5000 app:app
```

看到 `Listening at: http://0.0.0.0:5000` 即成功。`Ctrl+C` 停止。

---

## 四、Systemd 服务（生产持久运行）

创建服务文件：

```bash
nano /etc/systemd/system/llmrag.service
```

填入以下内容（**替换 `YOUR_DASHSCOPE_KEY` 和 `YOUR_METASO_KEY` 为真实密钥**）：

```ini
[Unit]
Description=LLM RAG Service
After=network.target mysql.service

[Service]
User=root
WorkingDirectory=/opt/llmrag
Environment="FLASK_ENV=prod"
Environment="DASHSCOPE_API_KEY=YOUR_DASHSCOPE_KEY"
Environment="METASO_API_KEY=YOUR_METASO_KEY"
Environment="MYSQL_HOST=127.0.0.1"
Environment="MYSQL_PORT=3306"
Environment="MYSQL_USER=llmrag"
Environment="MYSQL_PASSWORD=你的密码"
Environment="MYSQL_DATABASE=llmrag"
ExecStart=/opt/llmrag/.venv/bin/gunicorn -c /opt/llmrag/gunicorn.conf.py app:app
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
```

> `gunicorn.conf.py` 已配置：单 worker、gthread、180s 超时、端口 5000。直接用 `-c` 引用即可。

生效并启动：

```bash
systemctl daemon-reload
systemctl enable llmrag
systemctl start llmrag
systemctl status llmrag
```

常用运维命令：

```bash
systemctl status llmrag      # 查看状态
systemctl restart llmrag     # 重启
journalctl -u llmrag -f      # 实时日志
```

---

## 五、Nginx 反向代理

### 5.1 创建站点配置

```bash
nano /etc/nginx/sites-available/llmrag
```

填入：

```nginx
server {
    listen 80;
    server_name 47.92.74.156;

    client_max_body_size 50m;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # LLM 响应可能很慢，延长超时
        proxy_read_timeout 300s;
        proxy_connect_timeout 60s;
    }
}
```

### 5.2 启用并重启

```bash
ln -s /etc/nginx/sites-available/llmrag /etc/nginx/sites-enabled/llmrag
rm -f /etc/nginx/sites-enabled/default    # 可选：移除默认站点
nginx -t
systemctl restart nginx
```

---

## 六、阿里云安全组放行端口

在阿里云控制台 → ECS → 安全组 → 配置规则，添加入方向：

| 端口 | 协议 | 来源 | 用途 |
|---|---|---|---|
| 22 | TCP | 0.0.0.0/0 | SSH |
| 80 | TCP | 0.0.0.0/0 | HTTP 网页访问 |

> 5000 端口**不需要**开放——外部访问全部走 Nginx → 80 → 内网 5000。

---

## 七、验证部署

浏览器访问：

```
http://47.92.74.156
```

应看到项目前端页面。进一步验证：

```bash
# 健康检查
curl http://127.0.0.1:5000/api/keycheck

# 查看服务日志
journalctl -u llmrag -n 50
```

---

## 八、后续更新代码

推荐用 Git 方式，避免每次 scp 全量覆盖。

### 8.1 首次：把项目推送到 GitHub / Gitee

在**本地 Windows** 上（项目目录 `F:\code\llmrag`）：

```powershell
cd F:\code\llmrag

# 初始化 Git（如果项目还没 git init 过）
git init
git add .
git commit -m "init: LLM RAG project"

# 推送到远程仓库（以 GitHub 为例）
git remote add origin https://github.com/你的用户名/llmrag.git
git branch -M main
git push -u origin main
```

> 注意：推送前确认 `config/config_prod.py` 里的密钥已替换为占位符，或将该文件加入 `.gitignore`。建议新建一个 `.gitignore`，至少包含：
>
> ```
> .venv/
> __pycache__/
> *.pyc
> uploads/
> faiss_index_uploads/
> parsed_docs/
> generated_docs/
> .env
> config/config_prod.py
> ```

### 8.2 服务器上首次克隆

```bash
cd /opt
rm -rf llmrag                    # 删除之前 scp 上传的目录
git clone https://github.com/你的用户名/llmrag.git
cd llmrag

# 把 config_prod.py 单独传到服务器
# 方式一：scp 单文件
#   在本地 PowerShell: scp F:\code\llmrag\config\config_prod.py root@47.92.74.156:/opt/llmrag/config/
# 方式二：直接在服务器上创建 config_prod.py（推荐，密钥不落地到 Git）
nano /opt/llmrag/config/config_prod.py
# 粘贴真实配置，注意 DASHSCOPE_API_KEY 等用 os.environ.get() 不要写死
```

### 8.3 日常更新流程

```bash
# 本地改完代码后
git add .
git commit -m "描述你的改动"
git push

# 服务器上拉取并重启
cd /opt/llmrag
git pull
systemctl restart llmrag
```

如果依赖有变化：

```bash
source .venv/bin/activate
pip install -r requirements-prod.txt
systemctl restart llmrag
```

如果坚持用 scp 全量覆盖，更新后记得重启：

```bash
systemctl restart llmrag
```

---

## 九、关键配置说明

| 文件 | 作用 |
|---|---|
| `gunicorn.conf.py` | Gunicorn 配置：`workers=1`, `threads=4`, `timeout=180` |
| `wsgi.py` | Gunicorn 入口，等价于 `app:app` |
| `config/config_prod.py` | 生产配置，所有敏感值通过环境变量注入 |
| `app/__init__.py` | Flask 工厂，读取 `FLASK_ENV` 加载对应 config |
| `requirements-prod.txt` | 生产依赖（含 gunicorn） |

### 为什么只开 1 个 worker？

项目的会话记忆存储在进程内存中（`chat_memory_handler`），多个 worker 会导致同一会话的上下文漂移。已通过 `gunicorn.conf.py` 固定 `workers=1`，用 `threads=4` 补偿并发。

---

## 十、故障排查

| 现象 | 可能原因 | 处理 |
|---|---|---|
| 502 Bad Gateway | Gunicorn 未启动 | `systemctl status llmrag` 查看 |
| 前端页面空白 | `credentials.js` baseUrl 写死 | 已用 `window.location.origin`，无需改 |
| LLM 调用超时 | DashScope 响应慢 | `gunicorn.conf.py` 已设 `timeout=180`，Nginx 设 `proxy_read_timeout=300s` |
| FAISS 加载报错 | OpenMP 冲突 | `__init__.py` 已有 Windows 兜底，Linux 一般无此问题 |
| MySQL 连接失败 | 数据库未初始化 | 检查 `setup_mysql.py` 是否执行、密码是否正确 |
