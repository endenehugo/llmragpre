# 求职助手 Agent 平台阶段开发清单

## 说明

这份清单是在《求职助手Agent平台开发文档》的基础上继续细化得到的，目标是把项目拆成可执行的阶段任务，方便你按阶段推进、验收和写进简历。

建议按照下面 4 个阶段推进：

1. 第一阶段：跑通核心链路
2. 第二阶段：补全求职业务能力
3. 第三阶段：增强产品可用性与展示效果
4. 第四阶段：简历包装与项目验收

---

## 第一阶段：跑通核心链路

### 阶段目标

完成“上传简历 + 输入 JD + 输出结构化分析结果”的最小可用版本。

### 本阶段交付结果

- 可以创建求职分析会话
- 可以上传简历并完成解析与索引
- 可以提交 JD 文本并得到结构化分析
- 可以得到一份简历评分结果
- 可以在前端看到分析结果和历史消息

### 开发清单

#### 1. 明确本阶段的数据流

- 梳理当前 `/conversation/*` 主链路
- 明确“会话 -> 上传简历 -> 解析文本 -> 建立索引 -> 输入 JD -> 返回评分”的调用路径
- 画一版简单的流程图，方便后续面试时讲解

#### 2. 新增 JD 解析能力

- 新建 `JobDescriptionService`
- 设计 JD 解析 prompt
- 约束输出字段：
  - `job_role`
  - `keywords`
  - `requirements`
  - `bonus_points`
  - `suggested_project_angles`
- 新增 `POST /job/analyze` 接口
- 完成参数校验和异常返回

#### 3. 新增简历评分能力

- 新建 `ResumeScoringService`
- 复用当前会话文档索引，读取用户上传的简历文本
- 设计评分输出 schema
- 约束评分维度：
  - 技能匹配度
  - 项目相关性
  - 表达质量
  - 岗位适配度
- 新增 `POST /resume/score` 接口
- 输出总分、维度分、优势、缺失点、建议

#### 4. 补充结果落库

- 设计 `job_analysis` 表
- 增加对应 entity / repository
- 保存以下结果：
  - JD 原文
  - 解析结果
  - 匹配得分
  - 优势与缺口
  - 建议列表
- 保证每次分析都能追溯

#### 5. 前端补最小展示能力

- 在当前页面增加 JD 输入区
- 在当前页面增加“开始分析”按钮
- 增加结构化展示面板：
  - 岗位名称
  - 关键词
  - 总分
  - 四维评分
  - 建议列表
- 保证分析结果在刷新后仍可从接口重新拉取

#### 6. 本阶段联调与自测

- 测试“只上传简历不输 JD”时的错误提示
- 测试“只输 JD 不上传简历”时的错误提示
- 测试简历解析失败时的错误提示
- 测试结构化结果是否能稳定返回
- 测试结果是否成功落库

### 本阶段完成标准

- 用户能在页面中上传简历并输入 JD
- 后端能稳定返回结构化分析和评分
- 数据库存有对应分析记录
- 你可以现场演示完整流程

---

## 第二阶段：补全求职业务能力

### 阶段目标

把项目从“简历分析器”升级成“求职助手 Agent”。

### 本阶段交付结果

- 支持项目经历优化
- 支持多轮追问
- 支持查看历史分析和面试记录

### 开发清单

#### 1. 新增项目经历优化能力

- 新建 `ProjectRewriteService`
- 设计项目优化 prompt
- 支持输出：
  - 原描述问题
  - 优化版本
  - Python 后端实习导向版本
  - Agent 实习导向版本
- 新增 `POST /resume/project/rewrite` 接口

#### 2. 新增模拟面试能力

- 新建 `InterviewSimulationService`
- 设计面试题生成 prompt
- 支持根据 JD + 简历内容生成问题
- 支持按目标方向出题：
  - Python 后端
  - Agent / AI 应用
- 支持输出：
  - 题目
  - 参考答题点
  - 追问建议

#### 3. 增加模拟面试会话持久化

- 设计 `interview_sessions` 表
- 设计 `interview_messages` 表
- 为模拟面试建立单独的历史消息结构
- 支持保存：
  - 初始问题
  - 用户回答
  - Agent 追问
  - 阶段总结

#### 4. 打通求职业务 Agent 编排

- 明确主 Agent 的职责边界
- 增加工具：
  - `jd_parser_tool`
  - `resume_score_tool`
  - `project_rewrite_tool`
  - `mock_interview_tool`
- 让 Agent 根据用户意图自动选择工具
- 支持“先分析，再优化，再模拟面试”的连续流程

#### 5. 补历史记录页面能力

- 在左侧会话区展示求职分析历史
- 支持查看历史 JD 分析
- 支持查看历史面试问答
- 支持按岗位或简历版本切换

#### 6. 本阶段联调与自测

- 测试项目优化接口输出是否稳定
- 测试模拟面试的多轮会话是否正常保存
- 测试 Agent 工具选择是否符合预期
- 测试历史记录重新加载是否完整

### 本阶段完成标准

- 用户可以上传简历、分析 JD、改写项目描述、进入模拟面试
- 多轮面试记录和历史分析都能查到
- 项目已经具备“求职助手 Agent”雏形

---

## 第三阶段：增强产品可用性与展示效果

### 阶段目标

补足亮点功能，让项目更像完整产品，而不是单点 Demo。

### 本阶段交付结果

- 支持岗位截图分析
- 支持简历版本对比
- 支持内置求职知识库
- 支持更好的结构化展示与导出

### 开发清单

#### 1. 增加岗位截图分析

- 复用当前图片上传与多模态识别能力
- 支持上传招聘截图
- 从截图中提取 JD 文本
- 自动进入 JD 解析与评分流程

#### 2. 增加简历版本管理

- 设计 `resume_versions` 表
- 建立“同一会话下多个简历版本”的映射关系
- 支持选择不同简历版本进行评分
- 支持查看不同版本分数变化

#### 3. 增加内置知识库

- 补充平台内置资料：
  - Python 后端面试题
  - MySQL / Redis / Linux 高频问题
  - Flask 工程化问题
  - Agent / RAG 常见原理问题
- 设计“用户简历优先、平台知识库次之”的检索策略

#### 4. 增加结果导出能力

- 支持导出分析结果
- 支持导出项目优化建议
- 支持导出模拟面试总结
- 可以优先做 Markdown 或 Word 导出

#### 5. 优化前端展示

- 增加结构化卡片展示
- 增加评分雷达图或分项图表
- 增加“缺失点 / 建议”醒目展示区域
- 增加面试进度和历史记录展示

#### 6. 增加异常与边界处理

- 处理空简历、空 JD、超长 JD
- 处理文档内容过短导致分析不稳定
- 处理模型输出格式异常
- 处理图片识别失败或图片地址非法

### 本阶段完成标准

- 项目具备明显的产品感
- 演示效果更强
- 结构化结果更适合截图放进简历或作品集

---

## 第四阶段：简历包装与项目验收

### 阶段目标

把项目整理成可投递、可讲解、可展示的成果。

### 本阶段交付结果

- 完整 README / 开发文档 / 演示说明
- 简历版项目描述
- 面试讲解提纲
- 演示素材与验收记录

### 开发清单

#### 1. 整理项目文档

- 更新 README 中的项目介绍
- 写清楚核心功能、技术栈、运行方式
- 补充接口说明
- 补充数据库表说明
- 补充架构图或流程图

#### 2. 准备演示脚本

- 准备一份示例简历
- 准备 2~3 份典型 JD
- 设计一套标准演示顺序：
  - 上传简历
  - 输入 JD
  - 查看评分
  - 改写项目描述
  - 进入模拟面试

#### 3. 输出简历项目描述

- 写一版偏 Python 后端的项目描述
- 写一版偏 Agent / AI 应用的项目描述
- 每版控制在 3~4 条 bullet point
- 突出工程化、数据流、Agent 编排和持久化能力

#### 4. 准备面试讲稿

- 为什么做这个项目
- 系统架构怎么分层
- RAG 为什么要引入
- Agent 工具调用怎么设计
- MySQL 在这里解决了什么问题
- 会话持久化如何支持模拟面试和历史复盘

#### 5. 做一次最终验收

- 检查所有主流程是否可跑通
- 检查历史数据是否可追溯
- 检查页面是否存在明显交互问题
- 检查文档和演示是否统一
- 确认至少有 1 套稳定的演示样例

### 本阶段完成标准

- 这个项目不只是“做完了”，而是已经适合写进简历和现场演示
- 你能从产品、后端、Agent、RAG 四个角度讲清楚它

---

## 推荐推进顺序

如果你时间有限，建议优先级如下：

1. 第一阶段全部完成
2. 第二阶段至少完成“项目优化 + 模拟面试”
3. 第三阶段优先做“岗位截图分析”或“简历版本对比”二选一
4. 第四阶段一定要做

---

## 最小可写进简历的版本

如果你只来得及做一个可投递版本，最低要求建议是：

- 简历上传与解析
- JD 解析
- 简历评分
- 项目经历优化
- 结果落库
- 历史记录查询

做到这里，已经足够写成一个比较完整的“求职助手 Agent 平台”项目。

---

## 最终建议

你在真正开发时，不要平均发力，而要遵循：

- **先做主链路**
- **再做可演示亮点**
- **最后做包装与答辩**

对你当前目标来说，最有价值的不是“功能最多”，而是“面试官一看就懂、一问你就能讲深”的版本。
